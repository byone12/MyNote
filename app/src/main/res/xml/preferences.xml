<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 设置的类别 -->
    <PreferenceCategory android:title="同步功能">

        <!-- 设置单选 -->
        <CheckBoxPreference
            android:key="auto_sync"
            android:summary="自动同步会每隔2分钟自动将数据备份到云"
            android:title="自动同步" />

    </PreferenceCategory>
    <PreferenceCategory android:title="客服功能">
        <EditTextPreference
            android:key="phone"
            android:title="手机号码"
            android:textSize="18sp"/>
    </PreferenceCategory>
    <PreferenceCategory android:title="定位">
        <ListPreference
            android:key="address"
            android:title="地区"
            android:dialogTitle="请选择："
            android:entries="@array/address"
            android:entryValues="@array/address_values"
            />
    </PreferenceCategory>
</PreferenceScreen>